// Write a function to insert element at index of array.
// let array = [21, 4, 5, -1];

// function insert(array, index, element) {
//   array.splice(index, 0, element);
//   return array;
// }

// console.log(insert(array, 3, 99));
